# Kube Release Doctor 诊断报告

## 基本信息

- Namespace: `prod`
- Deployment: `devops-cicd-demo`
- 生成时间: `2026-05-17 15:13:08`
- 报告路径: `examples/v0.2-secret-missing-critical-report.md`

### kubectl 命令

- `kubectl get deployment devops-cicd-demo -n prod -o json`
- `kubectl get pods -n prod -l app=devops-cicd-demo -o json`

## 环境预检

| 检查项 | 状态 | 命令 | 详情 |
| --- | --- | --- | --- |
| kubectl 可执行文件 | 通过（OK） | `shutil.which('kubectl')` | /usr/bin/kubectl |
| kubectl 客户端版本 | 通过（OK） | `kubectl version --client` | Client Version: v1.36.1 Kustomize Version: v5.8.1 |
| 集群 namespace 访问 | 通过（OK） | `kubectl get namespace prod` | NAME   STATUS   AGE prod   Active   2d20h |

## 当前状态

- 整体状态：严重异常（Critical）
- 检测到明确的当前发布故障。
- 请重点检查 rollout 状态、Pod 状态、镜像拉取状态，以及 Secret / ConfigMap 引用。

## 诊断摘要

- 整体状态：严重异常（Critical）
- Deployment desiredReplicas: `2`
- Deployment availableReplicas: `2`
- Deployment updatedReplicas: `1`
- Pod 总数: `3`，异常 Pod: `1`
- Service 匹配数: `1`
- Secret / ConfigMap 缺失数: `1`
- 当前故障 Events 数: `1`
- RestartCount 告警 Pod 数: `0`
- 已尝试采集最近日志 Pod 数: `1`

## Deployment 状态

- desiredReplicas: `2`
- availableReplicas: `2`
- updatedReplicas: `1`
- readyReplicas: `2`
- Pod Selector: `app=devops-cicd-demo`

### Conditions

| 类型（Type） | 状态（Status） | 原因（Reason） | 消息（Message） |
| --- | --- | --- | --- |
| Available | True | MinimumReplicasAvailable | Deployment has minimum availability. |
| Progressing | True | ReplicaSetUpdated | ReplicaSet "devops-cicd-demo-55b56b77f5" is progressing. |

## Pod 状态

| Pod | Phase | Container | Ready | 重启次数（Restarts） | 等待原因（Waiting Reason） | 问题（Problems） | 告警（Warnings） |
| --- | --- | --- | --- | --- | --- | --- | --- |
| devops-cicd-demo-55b56b77f5-wq4zl | Pending | devops-cicd-demo | False | 0 | CreateContainerConfigError | CreateContainerConfigError, Pending | - |
| devops-cicd-demo-7858c8bb5f-75cwr | Running | devops-cicd-demo | True | 0 |  | - | - |
| devops-cicd-demo-7858c8bb5f-b8wss | Running | devops-cicd-demo | True | 0 |  | - | - |

## Service 检查

- 命令: `kubectl get services -n prod -o json`
- 判断结果: Service selector 可匹配当前 Deployment 的 Pod 标签。

| Service | 类型（Type） | Selector | Ports |
| --- | --- | --- | --- |
| devops-cicd-demo-service | NodePort | app=devops-cicd-demo | name=-, port=80, targetPort=5000, protocol=TCP |

## Secret / ConfigMap 检查

| 类型 | 名称 | 状态（Status） | 错误 |
| --- | --- | --- | --- |
| configmap | devops-cicd-demo-config | 存在（Found） | - |
| secret | devops-cicd-demo-secret | 缺失或无权访问（Missing or inaccessible） | Error from server (NotFound): secrets "devops-cicd-demo-secret" not found |

## Events 分析


### Pod `devops-cicd-demo-55b56b77f5-wq4zl`

- 命令: `kubectl describe pod devops-cicd-demo-55b56b77f5-wq4zl -n prod`
- 分类: `当前故障分析（Current Failure Investigation）`
- 命中的 Events 错误行:
  - `Warning  Failed     11s (x2 over 12s)  kubelet            spec.containers{devops-cicd-demo}: Error: secret "devops-cicd-demo-secret" not found`

## 最近日志


### Pod `devops-cicd-demo-55b56b77f5-wq4zl`

- 命令: `kubectl logs devops-cicd-demo-55b56b77f5-wq4zl -n prod --tail=50`
- 错误: `Error from server (BadRequest): container "devops-cicd-demo" in pod "devops-cicd-demo-55b56b77f5-wq4zl" is waiting to start: CreateContainerConfigError`

## 可能根因

- Deployment updatedReplicas 小于 desiredReplicas，rollout 可能未完成。
- Deployment 引用的 Secret / ConfigMap 缺失或无权访问: secret/devops-cicd-demo-secret。
- 存在 CreateContainerConfigError，常见原因是 secretKeyRef / configMapKeyRef / envFrom 引用的 Secret、ConfigMap 或 key 缺失。
- 存在 Pending Pod，可能是调度、资源配额、节点容量、PVC 或镜像拉取前置条件阻塞。

## 建议修复

- 创建缺失的 Secret / ConfigMap，或修正 Deployment 中 env / envFrom 引用的名称和 key。
- 以下命令中 `<KEY>` 和 `<VALUE>` 是占位符，执行前请替换为应用真实需要的配置值。

缺失 Secret `devops-cicd-demo-secret` 的修复命令示例:

```bash
kubectl create secret generic devops-cicd-demo-secret \
  --from-literal=<KEY>=<VALUE> \
  -n prod
```
- 检查节点资源、taints / tolerations、affinity、resource requests、PVC 状态和 namespace quota。
